# LiteBooth - 轻量级自动售货系统

## 项目介绍

LiteBooth 是一个基于 Spring Boot 和 Vue 3 开发的轻量级自动售货/发卡系统。它包含用户端和管理端两个部分，支持商品展示、在线下单、自动发货、库存管理、订单管理等核心功能。

### 主要功能

#### 用户端 (User Frontend)
- **商品浏览**：展示商品列表，支持按分类筛选。
- **商品详情**：查看商品详细信息。
- **在线下单**：支持多种支付方式（模拟），下单后自动扣减库存。
- **订单查询**：通过邮箱和查询密码查询历史订单及卡密/内容。
- **自动发货**：支付成功后自动展示库存内容（如卡密、账号等）。

#### 管理端 (Admin Frontend)
- **仪表盘**：系统概览。
- **商品管理**：商品的增删改查，支持富文本详情。
- **商品组管理**：商品分类管理。
- **库存管理**：
    - 支持单条添加库存。
    - **批量上传**：支持通过 TXT 文件批量上传库存内容。
    - 库存状态管理（未售/已售）。
- **订单管理**：
    - 查看所有订单状态。
    - 订单筛选（按状态、关键词）。
    - 订单详情查看。
- **系统管理**：用户管理、角色管理、菜单管理等（基于 Vue Pure Admin）。

## 技术栈

### 后端 (Backend)
- **开发语言**：Java 17+
- **框架**：Spring Boot 3.x
- **ORM**：MyBatis-Plus
- **数据库**：MySQL 8.0
- **工具**：Lombok, Swagger/Knife4j (API 文档)

### 前端 (Frontend)
- **核心框架**：Vue 3 (Composition API)
- **构建工具**：Vite
- **语言**：TypeScript
- **UI 组件库**：Element Plus
- **状态管理**：Pinia
- **管理端模板**：Vue Pure Admin
- **HTTP 客户端**：Axios

## 部署说明

### 环境要求
- JDK 17+
- Node.js 18+
- MySQL 8.0+
- Maven 3.8+

### 后端部署
1. 进入 `backend` 目录。
2. 修改 `src/main/resources/application.yml` 中的数据库配置。
3. 运行 `mvn clean package` 打包。
4. 运行 `java -jar target/litebooth-backend.jar`。

### 前端部署
1. 进入 `frontend-admin` 或 `frontend-user` 目录。
2. 运行 `pnpm install` 安装依赖。
3. 运行 `pnpm build` 打包。
4. 将 `dist` 目录下的文件部署到 Nginx 或其他 Web 服务器。

### Docker 部署
项目包含 `docker-compose.yml`，可直接使用 Docker Compose 一键部署。
```bash
cd backend/docker
docker-compose up -d
```

---

## 简历项目经历参考

### 项目名称：LiteBooth 自动售货系统
**角色**：全栈工程师
**技术栈**：Java, Spring Boot, MyBatis-Plus, MySQL, Vue 3, TypeScript, Element Plus, Docker

**项目描述**：
独立设计并开发的一套轻量级自动售货系统，旨在解决虚拟商品（如激活码、账号等）的自动售卖与发货需求。系统分为用户端（C端）和管理端（B端），实现了从商品上架、库存管理到用户下单、自动发货的全流程业务闭环。

**主要工作与业绩**：
1.  **后端架构设计与实现**：
    -   基于 **Spring Boot** 和 **MyBatis-Plus** 搭建高效稳定的后端服务，设计并实现了 RESTful API 接口。
    -   设计数据库模型，包含商品(Item)、库存(Stock)、订单(Order)等核心表结构，利用索引优化查询性能。
    -   实现了**高并发下的库存扣减**逻辑，通过数据库事务保证数据一致性，防止超卖现象。
    -   开发了**批量库存导入**功能，支持解析大文件文本并批量插入数据库，大幅提升了运营效率。
    -   集成了 Swagger/Knife4j 自动生成接口文档，规范了前后端交互标准。

2.  **前端开发（管理端 & 用户端）**：
    -   **管理端**：基于 **Vue Pure Admin** 开源模板进行二次开发，快速搭建了具备权限控制的后台管理系统。
    -   实现了复杂的表格交互，如**库存管理的二级列表展示**（商品-库存），支持展开查看详情、单条/批量编辑与删除。
    -   封装了通用的 Axios 请求工具与 Hooks，统一处理请求响应与错误，提升了代码复用率。
    -   **用户端**：使用 Vue 3 + Element Plus 开发响应式用户界面，实现了流畅的购物体验与订单查询功能。

3.  **核心业务模块攻克**：
    -   **订单模块**：实现了订单创建、支付状态流转（待支付->已支付->已发货）及自动发货逻辑。
    -   **库存模块**：设计了灵活的库存添加策略，支持单项录入与文件批量上传，并实现了库存状态（未售/已售）的实时更新。

**个人优势（融合版）**：
1.  **Java 全栈开发能力**：具备扎实的 Java 基础，熟练掌握 Spring Boot 生态，同时具备 Vue 3 + TypeScript 前端开发能力，能够独立完成前后端全链路开发，理解完整的 Web 开发流程。
2.  **工程化与规范意识**：在项目中注重代码规范与模块划分，熟练使用 Git 进行版本控制，善于利用 Swagger 等工具提升开发协作效率。
3.  **问题解决与实战经验**：具备独立从 0 到 1 开发完整项目的经验，能够处理数据库设计、接口开发、前后端联调中遇到的各种问题，关注系统的扩展性与可维护性。
4.  **高效利用 AI 工具**：在开发过程中熟练运用 GitHub Copilot 等 AI 工具辅助编码、生成样板代码及优化逻辑，显著提升了开发效率与代码质量。
5.  **快速学习与适应**：能够快速上手新技术（如 Vue Pure Admin 模板），并将其灵活应用于实际项目中，适应多变的需求与开发环境。

