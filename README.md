# LiteBooth —— 个人小店系统 (虚拟商品自动发货)

## 📖 项目介绍

LiteBooth 是一个基于前后端分离架构的轻量级个人电商系统，专为**虚拟商品**（如激活码、账号、卡密等）的自动发货场景设计。

项目包含三个核心子工程：
- **User Frontend**: 面向消费者的响应式商城，支持商品浏览、搜索、下单及自助查单。
- **Admin Frontend**: 功能完善的商家管理后台，提供商品管理、库存（卡密）导入、订单监控及店铺装修功能。
- **Backend**: 基于 Spring Boot 3 的高性能后端服务，提供 RESTful API 及核心业务逻辑支撑。

本项目适合作为全栈开发的学习案例或个人作品集展示，涵盖了从数据库设计、后端开发、前端实现到容器化部署的完整流程。

## 🛠 技术栈

### 后端 (Backend)
- **开发语言**: Java 21
- **核心框架**: Spring Boot 3
- **ORM 框架**: MyBatis-Plus
- **数据库**: MySQL 8.0
- **缓存**: Redis (缓存热点数据、验证码 Token)
- **对象存储**: MinIO (自建对象存储，用于商品图片管理)
- **API 文档**: Knife4j (OpenAPI 3)
- **安全/认证**: JWT (双令牌机制), Hutool (图形验证码)
- **工具库**: Lombok, Hutool

### 前端 (Frontend)
- **框架**: Vue 3 (Composition API)
- **构建工具**: Vite
- **语言**: TypeScript
- **UI 组件库**: Element Plus
- **CSS 框架**: TailwindCSS
- **状态管理**: Pinia
- **HTTP 客户端**: Axios
- **管理端脚手架**: 基于 Vue Pure Admin

### 部署与运维
- **容器化**: Docker, Docker Compose

## 📂 项目结构

```
litebooth/
├── backend/            # 后端工程 (Spring Boot)
│   ├── docker/         # 中间件编排 (MySQL, Redis, MinIO)
│   └── src/            # 业务源码
├── frontend-admin/     # 商家管理后台 (Vue 3 + Element Plus)
│   └── src/views/      # 包含商品、库存、订单、店铺设置等管理页面
├── frontend-user/      # 用户商城前台 (Vue 3)
│   └── src/views/      # 包含首页、商品详情、查单页
├── pom.xml             # Maven 父工程配置
└── README.md           # 项目说明文档
```

## ✨ 主要功能

### 🛍️ 用户端
- **商品展示**: 支持按分组筛选、关键词搜索商品；详情页展示库存状态。
- **自助下单**: 填写邮箱与查询密码即可下单，支持图形验证码校验，防止恶意刷单。
- **自动发货**: 支付成功后（模拟），系统自动从库存中提取卡密/内容并展示给用户。
- **订单查询**: 用户可通过邮箱和查询密码自助查询历史订单及卡密信息。

### 💻 管理端
- **仪表盘**: 查看系统运行状态。
- **商品管理**: 商品增删改查，支持上传封面图（自动去重）、设置价格与描述。
- **库存管理**: 核心功能。支持导入虚拟商品库存（如卡密），系统自动管理库存状态（未售/已售）。
- **订单管理**: 查看所有订单详情，支持按状态筛选。
- **店铺装修**: 配置店铺名称、Logo、公告等基础信息。
- **安全中心**: 管理员账号管理，登录日志等。

## 🚀 快速开始 (部署指南)

### 前置要求
- JDK 21+
- Node.js 18+ & pnpm
- Docker & Docker Compose

### 1. 启动基础服务 (MySQL, Redis, MinIO)

进入后端 Docker 目录并启动服务：

```bash
cd backend/docker
docker-compose up -d
```

> **注意**: 请检查 `backend/src/main/resources/application.yml` 中的数据库、Redis 和 MinIO 连接配置。默认配置可能使用了特定的 IP 地址 (`172.31.241.156`)，请根据你的本地环境（如 `localhost`）进行修改。

### 2. 启动后端服务

```bash
cd backend
# 使用 Maven Wrapper 运行
./mvnw spring-boot:run
```

后端服务默认运行在 `8080` 端口。
API 文档地址: `http://localhost:8080/doc.html`

### 3. 启动管理后台 (Frontend Admin)

```bash
cd frontend-admin
pnpm install
pnpm dev
```

### 4. 启动用户端 (Frontend User)

```bash
cd frontend-user
pnpm install
pnpm dev
```
